<div class="mb-4">
  <h1 class="mb-3">크롤링 결과: "<%= keyword %>"</h1>
  <p>검색 결과 <%= movies.length %>개의 영화를 찾았습니다.</p>
  <a href="/crawl-movies" class="btn btn-outline-secondary mb-4">
    <i class="fas fa-arrow-left me-2"></i>다시 검색
  </a>
</div>

<% if (movies && movies.length > 0) { %>
  <div class="row">
    <% movies.forEach((movie, index) => { %>
      <div class="col-md-6 mb-4">
        <div class="card h-100">
          <div class="row g-0">
            <div class="col-md-4">
              <% if (movie.posterUrl) { %>
                <img src="<%= movie.posterUrl %>" class="img-fluid rounded-start h-100 w-100" style="object-fit: cover;" alt="<%= movie.title %> 포스터">
              <% } else { %>
                <div class="bg-secondary d-flex justify-content-center align-items-center h-100">
                  <i class="fas fa-film fa-3x text-white"></i>
                </div>
              <% } %>
            </div>
            <div class="col-md-8">
              <div class="card-body">
                <h5 class="card-title"><%= movie.title %></h5>
                <p class="card-text">
                  <% if (movie.director) { %><small>감독: <%= movie.director %></small><br><% } %>
                  <% if (movie.year) { %><small>개봉: <%= movie.year %></small><br><% } %>
                  <% if (movie.genre) { %><small>장르: <%= movie.genre %></small><% } %>
                </p>
                <% if (movie.synopsis) { %>
                  <p class="card-text"><small><%= movie.synopsis.length > 100 ? movie.synopsis.substring(0, 100) + '...' : movie.synopsis %></small></p>
                <% } %>
                
                <form action="/save-crawled-movie" method="POST" class="mt-3">
                  <input type="hidden" name="title" value="<%= movie.title %>">
                  <input type="hidden" name="director" value="<%= movie.director %>">
                  <input type="hidden" name="year" value="<%= movie.year %>">
                  <input type="hidden" name="genre" value="<%= movie.genre %>">
                  <input type="hidden" name="synopsis" value="<%= movie.synopsis %>">
                  <input type="hidden" name="posterUrl" value="<%= movie.posterUrl %>">
                  
                  <button type="submit" class="btn btn-primary btn-sm">
                    <i class="fas fa-plus me-1"></i>사이트에 추가
                  </button>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% }) %>
  </div>
<% } else { %>
  <div class="alert alert-warning">
    <p class="mb-0">검색 결과가 없습니다. 다른 키워드로 다시 시도해보세요.</p>
  </div>
<% } %>