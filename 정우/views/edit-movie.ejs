<div class="row">
  <div class="col-lg-8 mx-auto">
    <h1 class="mb-4">영화 정보 수정</h1>
    
    <div class="card">
      <div class="card-body">
        <form action="/edit-movie/<%= movie._id %>" method="POST" enctype="multipart/form-data">
          <div class="mb-3">
            <label for="title" class="form-label">영화 제목</label>
            <input type="text" class="form-control" id="title" name="title" value="<%= movie.title %>" required>
          </div>
          
          <div class="mb-3">
            <label for="director" class="form-label">감독</label>
            <input type="text" class="form-control" id="director" name="director" value="<%= movie.director %>" required>
          </div>
          
          <div class="row mb-3">
            <div class="col-md-6">
              <label for="year" class="form-label">개봉년도</label>
              <input type="number" class="form-control" id="year" name="year" min="1900" max="2099" value="<%= movie.year %>" required>
            </div>
            <div class="col-md-6">
              <label for="genre" class="form-label">장르</label>
              <input type="text" class="form-control" id="genre" name="genre" value="<%= movie.genre %>" required>
            </div>
          </div>
          
          <div class="mb-3">
            <label for="synopsis" class="form-label">줄거리</label>
            <textarea class="form-control" id="synopsis" name="synopsis" rows="4" required><%= movie.synopsis %></textarea>
          </div>
          
          <% if (movie.posterPath) { %>
            <div class="mb-3">
              <label class="form-label">현재 포스터</label>
              <div>
                <img src="<%= movie.isExternalPoster ? movie.posterPath : movie.posterPath %>" alt="<%= movie.title %> 포스터" class="img-thumbnail" style="max-height: 200px">
              </div>
            </div>
          <% } %>
          
          <div class="mb-3">
            <label for="poster" class="form-label">새 포스터 이미지 (선택사항)</label>
            <input type="file" class="form-control" id="poster" name="poster" accept="image/*">
            <div class="form-text">새 이미지를 업로드하면 기존 이미지가 대체됩니다.</div>
          </div>
          
          <div class="mb-4">
            <label for="posterUrl" class="form-label">포스터 이미지 URL (선택사항)</label>
            <input type="url" class="form-control" id="posterUrl" name="posterUrl" placeholder="https://example.com/image.jpg" value="<%= movie.isExternalPoster ? movie.posterPath : '' %>">
          </div>
          
          <div class="d-flex justify-content-between">
            <a href="/movie/<%= movie._id %>" class="btn btn-secondary">취소</a>
            <button type="submit" class="btn btn-primary">변경사항 저장</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>