<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>이메일 주소록</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
  <style>
    .container { max-width: 800px; margin-top: 30px; }
    .card { margin-bottom: 20px; }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="mb-4">이메일 주소록</h1>
    
    <div class="d-flex justify-content-between mb-4">
      <a href="/add-contact" class="btn btn-primary">새 연락처 추가</a>
      <a href="/email-history" class="btn btn-secondary">이메일 발송 기록</a>
    </div>
    
    <% if (contacts.length === 0) { %>
      <div class="alert alert-info">
        등록된 연락처가 없습니다. 새 연락처를 추가해보세요!
      </div>
    <% } else { %>
      <div class="row">
        <% contacts.forEach(contact => { %>
          <div class="col-md-6">
            <div class="card">
              <div class="card-body">
                <h5 class="card-title"><%= contact.name %></h5>
                <p class="card-text">
                  <strong>이메일:</strong> <%= contact.email %><br>
                  <strong>전화번호:</strong> <%= contact.phone || '없음' %><br>
                  <strong>그룹:</strong> <%= contact.group || '미지정' %>
                </p>
                <div class="d-flex">
                  <a href="/contact/<%= contact._id %>" class="btn btn-sm btn-info me-2">상세보기</a>
                  <a href="/send-email/<%= contact._id %>" class="btn btn-sm btn-success me-2">이메일 보내기</a>
                  <a href="/edit-contact/<%= contact._id %>" class="btn btn-sm btn-warning me-2">수정</a>
                  <form action="/delete-contact/<%= contact._id %>" method="POST" onsubmit="return confirm('정말 삭제하시겠습니까?');">
                    <button type="submit" class="btn btn-sm btn-danger">삭제</button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } %>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>