<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>연락처 수정</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
  <style>
    .container { max-width: 600px; margin-top: 30px; }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="mb-4">연락처 수정</h1>
    
    <form action="/update-contact/<%= contact._id %>" method="POST">
      <div class="mb-3">
        <label for="name" class="form-label">이름</label>
        <input type="text" class="form-control" id="name" name="name" value="<%= contact.name %>" required>
      </div>
      
      <div class="mb-3">
        <label for="email" class="form-label">이메일</label>
        <input type="email" class="form-control" id="email" name="email" value="<%= contact.email %>" required>
      </div>
      
      <div class="mb-3">
        <label for="phone" class="form-label">전화번호</label>
        <input type="tel" class="form-control" id="phone" name="phone" value="<%= contact.phone || '' %>">
      </div>
      
      <div class="mb-3">
        <label for="group" class="form-label">그룹</label>
        <select class="form-select" id="group" name="group">
          <option value="" <%= !contact.group ? 'selected' : '' %>>그룹 선택</option>
          <option value="가족" <%= contact.group === '가족' ? 'selected' : '' %>>가족</option>
          <option value="친구" <%= contact.group === '친구' ? 'selected' : '' %>>친구</option>
          <option value="직장" <%= contact.group === '직장' ? 'selected' : '' %>>직장</option>
          <option value="학교" <%= contact.group === '학교' ? 'selected' : '' %>>학교</option>
          <option value="기타" <%= contact.group === '기타' ? 'selected' : '' %>>기타</option>
        </select>
      </div>
      
      <div class="d-flex justify-content-between">
        <button type="submit" class="btn btn-primary">저장</button>
        <a href="/contact/<%= contact._id %>" class="btn btn-secondary">취소</a>
      </div>
    </form>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>