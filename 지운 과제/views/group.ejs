<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= groupName %> 그룹 연락처</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
  <style>
    .container { max-width: 800px; margin-top: 30px; }
    .card { margin-bottom: 20px; }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="mb-4"><%= groupName %> 그룹 연락처</h1>
    
    <div class="mb-3">
      <a href="/" class="btn btn-secondary">전체 연락처로 돌아가기</a>
    </div>
    
    <% if (contacts.length === 0) { %>
      <div class="alert alert-info">
        이 그룹에 등록된 연락처가 없습니다.
      </div>
    <% } else { %>
      <div class="row">
        <% contacts.forEach(contact => { %>
          <div class="col-md-6">
            <div class="card">
              <div class="card-body">
                <h5 class="card-title"><%= contact.name %></h5>
                <p class="card-text">
                  <strong>이메일:</strong> <%= contact.email %><br>
                  <strong>전화번호:</strong> <%= contact.phone || '없음' %>
                </p>
                <div class="d-flex">
                  <a href="/contact/<%= contact._id %>" class="btn btn-sm btn-info me-2">상세보기</a>
                  <a href="/send-email/<%= contact._id %>" class="btn btn-sm btn-success me-2">이메일 보내기</a>
                </div>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } %>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>