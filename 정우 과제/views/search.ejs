<div class="mb-4">
  <h1 class="mb-3">영화 검색</h1>
  
  <form action="/search" method="GET" class="mb-4">
    <div class="input-group">
      <input type="text" class="form-control" name="query" placeholder="영화 제목, 감독, 장르로 검색..." value="<%= typeof query !== 'undefined' ? query : '' %>">
      <button class="btn btn-primary" type="submit">
        <i class="fas fa-search me-1"></i> 검색
      </button>
    </div>
  </form>
</div>

<% if (typeof movies !== 'undefined' && movies.length > 0) { %>
  <div class="mb-3">
    <p class="lead">"<%= query %>" 검색 결과: <%= movies.length %>개의 영화를 찾았습니다.</p>
  </div>
  
  <div class="row row-cols-1 row-cols-md-3 row-cols-lg-4 g-4">
    <% movies.forEach(movie => { %>
      <div class="col">
        <div class="card h-100 movie-card">
          <div class="poster-container">
            <% if (movie.posterPath) { %>
              <img src="<%= movie.isExternalPoster ? movie.posterPath : movie.posterPath %>" class="card-img-top" alt="<%= movie.title %> 포스터">
            <% } else { %>
              <div class="bg-secondary d-flex justify-content-center align-items-center h-100">
                <i class="fas fa-film fa-3x text-white"></i>
              </div>
            <% } %>
          </div>
          <div class="card-body">
            <h5 class="card-title"><%= movie.title %></h5>
            <p class="card-text">
              <small>감독: <%= movie.director %></small><br>
              <small>개봉: <%= movie.year %></small><br>
              <small>장르: <%= movie.genre %></small>
            </p>
            <div class="d-flex justify-content-between align-items-center">
              <div class="rating-stars">
                <% for(let i = 1; i <= 5; i++) { %>
                  <% if (i <= Math.round(movie.rating)) { %>
                    <i class="fas fa-star"></i>
                  <% } else { %>
                    <i class="far fa-star"></i>
                  <% } %>
                <% } %>
                <span class="ms-1"><%= movie.rating %>/5</span>
              </div>
              <span class="badge bg-primary rounded-pill"><%= movie.reviewCount %> 리뷰</span>
            </div>
          </div>
          <div class="card-footer">
            <a href="/movie/<%= movie._id %>" class="btn btn-primary w-100">상세보기</a>
          </div>
        </div>
      </div>
    <% }) %>
  </div>
<% } else if (typeof query !== 'undefined') { %>
  <div class="alert alert-info">
    <p class="mb-0">"<%= query %>"에 대한 검색 결과가 없습니다.</p>
    <p class="mt-2 mb-0">다른 키워드로 다시 검색하거나 <a href="/crawl-movies" class="alert-link">영화 크롤링</a>을 이용해보세요.</p>
  </div>
<% } %>